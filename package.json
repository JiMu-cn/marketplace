{
  "name": "jimu-resource",
  "version": "1.0.0",
  "private": true,
  "description": "积木平台资源仓库 - Skills / Agents / MCPs / Teams",
  "scripts": {
    "build": "node scripts/build-all.js",
    "validate": "node scripts/validate-registry.js",
    "reset": "node scripts/build-all.js --reset"
  }
}
